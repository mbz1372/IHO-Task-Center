# V12 CRM 360 Fix

- Count کل هتل‌ها با `count: exact` از `ihos_hotels` خوانده می‌شود.
- کارت‌های CRM 360 بین ستون‌ها Drag & Drop می‌شوند.
- مرحله هر هتل در ستون `crm_stage` ذخیره می‌شود.
- دکمه `+` کنار نام هر هتل، فرم ایجاد تسک را با همان هتل باز می‌کند.

قبل از استفاده، فایل `SUPABASE-V12-CRM-PIPELINE.sql` را در Supabase SQL Editor اجرا کنید.
